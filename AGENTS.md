Always notify if using the GPU.
When giving a powershell command to run, keep it on one line
when giving a ticker such as "$SPY" exclude the "$" dollar sign please so it would just be "SPY"

Living summary policy: At the end of every response, append an entry to `LIVING_SUMMARY.md` at the repo root using `{date/time EST}{user: codex or claude}{brief description of discussion or changes}`. Keep each message summary to no more than 3 lines.
When starting work in a new chat, read `LIVING_SUMMARY.md` first if it exists so current context carries across Codex and Claude sessions.
Prefer durable facts, decisions, open questions, commands run, files changed, and next steps. Avoid copying large command outputs, secrets, or noisy transient details.
