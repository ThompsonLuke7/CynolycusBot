Verification policy: Never run test suites, training, eval, or any command expected to take >2min unless explicitly requested by the user. Prefer static checks only.

When giving a powershell command to run, keep it on one line